import { Box } from "@mantine/core";
import type { ContextModalProps } from "@mantine/modals";
import { Task } from "~/types";

export const AddAssignee = ({ context, id, innerProps, task }: (ContextModalProps & { task: Task })) => (
  <Box>

  </Box>
);